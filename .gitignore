# Build artifacts
*.zip
dist/

# Wrangler
.wrangler
node_modules/

# Secrets — never commit
.env*
.dev.vars*
nexus.mods.bypass/.claude/settings.local.json
.claude/settings.local.json
/internal/promo
/LeadManagerUI

# Reference userscripts to port from — not part of the package
you need to add this (gitgnore this folder)/
nexusmods

# Development & internal tools
tools/
